import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({
  navbar: {
    overflow: 'hidden',
    backgroundColor: '#eee',
    color: "#fff",
    position: 'fixed',
    bottom: 0,
  },
  link: {
    textDecoration: 'none',
    textDecorationColor: '#fff',
    color: '#fff'
  }
}));
