import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({
  title: {
    fontSize: 62,
    fontWeight: 900,
    letterSpacing: -2,
  },
  subtitle: {
    fontSize: 18,
  },
  loginbox: {
    marginBottom: 40,
    fontSize: 30,

  },
  loginbutton: {
    fontSize: 16,
  }
}));
