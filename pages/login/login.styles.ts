import { createStyles } from '@mantine/core';

export default createStyles((theme) => ({

  title: {
    fontSize: 62,
    fontWeight: 900,
    letterSpacing: -2,
    textAlign: 'center',
  },
  subtitle: {
    fontSize: 18,
    textAlign: 'center',
  },
  loginbox: {
    marginBottom: 40,
    margin: 30,
    fontSize: 30,

  },
  loginbutton: {
    fontSize: 16,
  }
}));
